# Touchpad Keyboard + Receiver (Source + Latest Build)

This repository contains:
- Android keyboard source (`android_app/app`)
- Android touchpad source (`android_app/touchpad`)
- Shared Android networking module (`android_app/shared_net`)
- Windows receiver source (`windows_receiver`, `windows_receiver_winui`)
- Latest build artifacts in `latest-build/`

## Latest Build Artifacts
- `latest-build/keyboard/app-release-signed.apk`
- `latest-build/touchpad/touchpad-debug.apk`
- `latest-build/receiver/Receiver_WinUI.exe`

Checksums and timestamps are in `latest-build/manifest.json`.

## Notes
- Build binaries are tracked with Git LFS (`*.apk`, `*.exe`).
- Upstream full README is copied as `README.upstream.md`.
